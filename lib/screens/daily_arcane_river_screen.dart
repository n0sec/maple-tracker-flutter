import 'package:flutter/material.dart';

class DailyArcaneRiverScreen extends StatelessWidget {
  const DailyArcaneRiverScreen({Key? key, required this.title}) : super(key: key);

  final String title;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
