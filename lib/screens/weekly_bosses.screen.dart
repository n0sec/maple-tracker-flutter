import 'package:flutter/material.dart';

class WeeklyBossesScreen extends StatelessWidget {
  const WeeklyBossesScreen({Key? key, required this.title}) : super(key: key);

  final String title;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
